{
  "name": "@cryptix/grpc-node",
  "version": "0.0.7",
  "description": "Node gRPC for cryptix-wallet",
  "main": "dist/index.js",
  "authors": [
    {
      "name": "ASPECTRON Inc."
    },
    {
      "name": "Cryptix"
    }
  ],
  "license": "ISC",
  "scripts": {
    "watch": "tsc --watch",
    "prepublishOnly": "rm -rf ./dist && rm -f package-lock.json && npm install --also=dev && tsc",
    "prepare": "tsc"
  },
  "dependencies": {
    "@cryptix/grpc": "https://github.com/cryptix-network/cryptix-grpc"
  },
  "devDependencies": {
    "@types/node": "^14.14.9",
    "typescript": "^4.0.5"
  },
  "files": [
    "dist",
    "lib",
    "types",
    "index.ts",
    "tsconfig.json"
  ]
}
